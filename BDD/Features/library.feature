Feature: DatabasePS
    User use the library module to add the products
    models, and figure to them project
    if user register as vendor then user products can
    presents for all the users


Scenario: User access libraly as normal user
    Given User on the main page
    When User click on library button
    Then library module is diplayd
    And User name is displayd
     When User clik on arrow
     And click on the plus button
     And enter the structure name
     And click on create button